<div class="card-container">
  <div class="card w-50 mx-auto mt-4 shadow-lg">
    <div class="card-body">
    <h1>画像投稿フォーム</h1>
 <h4>※ゲストログイン時は投稿できません</h4>
<%= form_with model: @post_image do |f| %>
  <h4>画像</h4>
  <%= f.file_field :image, accept: "image/*" %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>説明</h4>
  <%= f.text_area :body %>
  <%= f.submit '投稿' %>
<% end %>
</div>
</div>